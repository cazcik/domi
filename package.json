{
  "name": "domi",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:www": "pnpm run build --filter www",
    "build:app": "pnpm run build --filter app",
    "deploy:www": "pnpm run deploy --filter www",
    "deploy:app": "pnpm run deploy --filter app"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  },
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
